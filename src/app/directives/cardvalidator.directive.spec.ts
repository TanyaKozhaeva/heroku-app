import { CardvalidatorDirective } from './cardvalidator.directive';

describe('CardvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
