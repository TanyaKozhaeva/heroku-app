import { UniquePhoneDirective } from './unique-phone.directive';

describe('UniquePhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new UniquePhoneDirective();
    expect(directive).toBeTruthy();
  });
});
