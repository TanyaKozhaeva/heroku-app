import { ExpiredvalidatorDirective } from './expiredvalidator.directive';

describe('ExpiredvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpiredvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
